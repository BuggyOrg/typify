{
  "name": "@buggyorg/typify",
  "version": "0.1.3",
  "description": "Applies a type scheme to a weakly typed buggy graph.",
  "main": "lib/api.js",
  "scripts": {
    "test": "mocha --compilers js:babel-register && standard src/**/*.js",
    "build": "babel src -d lib && babel test -d test/lib",
    "prepublish": "babel src -d lib"
  },
  "author": "Maximilian Klein",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.6.0",
    "babel-register": "^6.7.2",
    "chai": "^3.5.0",
    "mocha": "^2.4.5",
    "standard": "^6.0.8"
  },
  "dependencies": {
    "@buggyorg/graphtools": "^0.4.0-pre.7",
    "graphlib": "^2.1.0",
    "lodash": "^4.11.1"
  }
}
